# Fixtures

- `listas-clasificacion-concentradora.xlsx` — copia de OneDrive `Pruebas MMI` (órdenes PM / ubicaciones técnicas, ~28k filas). Usar con `mmi.tools.preview_excel`.

Los `*.xlsx` / `*.pdf` de esta carpeta sí se versionan; el resto del disco está en `.gitignore`.
