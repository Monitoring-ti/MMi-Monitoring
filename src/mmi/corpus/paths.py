"""Rutas por defecto del corpus local."""

from __future__ import annotations

from pathlib import Path

# Corpus operativo MMi (NCC30 + resto de documentación ODS1)
DEFAULT_CORPUS = Path("ODS1 TORR ENF DCH")
DEFAULT_EXTRACT_FULL = Path("out/ods1-extract")
DEFAULT_EXTRACT_LOTE1 = Path("out/lote1-extract")
