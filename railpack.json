{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "sh deploy/entrypoint.sh"
  },
  "steps": {
    "build": {
      "commands": [
        "...",
        "mkdir -p out",
        "cp deploy/railway-seed/*.json out/ || true",
        "MMI_DEPLOY_MODE=vitrina python -m mmi.tools.vitrina"
      ]
    }
  }
}
